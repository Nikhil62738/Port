# SEO Implementation Plan (2026)

## Information gathered
- Read `robots.txt`: allows all `/` and references `https://nikhilchopade.com/sitemap.xml`.
- Read `sitemap.xml`: includes only the homepage URL.
- Read `index.html`: already has title/meta description/canonical/OpenGraph/Twitter + `Person` schema; mostly single-page with sections (Home/About/Resume/Projects/Contact).

## TODO steps (in order)
1. Confirm live domain + that sitemap submission in GSC shows success.
2. Run PageSpeed Insights (mobile) and note CWV/performance blockers.
3. Update/verify on-page SEO:
   - refine meta title + meta description for top intent keywords + location
   - ensure one clear H1 (already present) and consistent H2/H3 usage
4. Rewrite About + Projects copy to be specific (problem → action → result) and keyword-aligned.
5. Improve image SEO:
   - add missing `alt` for images where `<img>` exists
   - plan WebP + compression for hero/project images
6. Upgrade schema markup:
   - enhance `Person` schema (description + image URL if available)
7. Add content expansion plan:
   - create 2–4 blog posts on the domain targeting long-tail queries
   - link blog posts to relevant project cards/sections
8. Build backlinks and social proof weekly.
9. Monitor in GSC weekly (Queries, Indexing, CWV) and iterate.

